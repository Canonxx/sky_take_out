<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hubu.mapper.CategoryMapper">
<!--        void insert(Category category);-->
    <insert id="insert" parameterType="com.hubu.entity.Category">
        insert into category(type,name,sort,status,create_time,update_time,create_user,update_user)
            values (#{type},#{name},#{sort},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
<!--    Page<Category> find(Category category);-->
    <select id="find" resultType="com.hubu.entity.Category" parameterType="com.hubu.entity.Category">
        select *
        from category
        <where>
            <if test="id != null">id = #{id} </if>
            <if test="type != null">and `type` = #{type} </if>
            <if test="name != null and name != ''">and name like concat('%',#{name},'%') </if>
            <if test="sort != null">and sort = #{sort} </if>
            <if test="status != null">and status = #{status} </if>
            <if test="createTime != null">and create_time = #{createTime} </if>
            <if test="updateTime != null">and update_time = #{updateTime} </if>
            <if test="createUser != null">and create_user = #{createUser} </if>
            <if test="updateUser != null">and update_user = #{updateUser}</if>
        </where>
        order by sort asc,update_time desc
    </select>
<!--    void update(Category category);-->
    <update id="update" parameterType="com.hubu.entity.Category">
        update category
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            <!-- 自动更新时间-->
            update_time = NOW()
        </set>
        where id = #{id}
    </update>
<!--    void deleteById(Long id);-->
    <delete id="deleteById">
        delete from category
        where id = #{id}
    </delete>
</mapper>
