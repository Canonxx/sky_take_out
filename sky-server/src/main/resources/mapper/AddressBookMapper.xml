<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hubu.mapper.AddressBookMapper">
<!--    void insert(AddressBook addressBook);-->
    <insert id="insert" parameterType="com.hubu.entity.AddressBook" useGeneratedKeys="true" keyProperty="id">
        insert into address_book
            (user_id, consignee, sex, phone, province_code, province_name,
             city_code, city_name, district_code, district_name, detail, label, is_default)
        values
            (#{userId}, #{consignee}, #{sex}, #{phone}, #{provinceCode}, #{provinceName},
             #{cityCode}, #{cityName}, #{districtCode}, #{districtName}, #{detail}, #{label}, #{isDefault})
    </insert>

<!--    List<AddressBook> listByUserId(Long userId);-->
    <select id="listByUserId" resultType="com.hubu.entity.AddressBook">
        select * from address_book
        where user_id = #{userId}
        order by id desc
    </select>

<!--    void clearDefaultByUserId(Long userId);-->
    <update id="clearDefaultByUserId">
        update address_book set is_default = 0
        where user_id = #{userId}
    </update>

<!--    void setDefaultById(Long id);-->
    <update id="setDefaultById">
        update address_book set is_default = 1
        where id = #{id}
    </update>
<!--        AddressBook getById(Long id);-->
    <select id="getById" resultType="com.hubu.entity.AddressBook">
        select * from address_book
        where id = #{id}
    </select>
<!--        void update(AddressBook addressBook);-->
    <update id="update">
        update address_book
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="consignee != null">consignee = #{consignee},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="provinceCode != null">province_code = #{provinceCode},</if>
            <if test="provinceName != null">province_name = #{provinceName},</if>
            <if test="cityCode != null">city_code = #{cityCode},</if>
            <if test="cityName != null">city_name = #{cityName},</if>
            <if test="districtCode != null">district_code = #{districtCode},</if>
            <if test="districtName != null">district_name = #{districtName},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="label != null">label = #{label},</if>
            <if test="isDefault != null">is_default = #{isDefault}</if>
        </set>
        where id = #{id}
    </update>
<!--        void deleteById(Long id);-->
    <delete id="deleteById">
        delete from address_book
        where id = #{id}
    </delete>
</mapper>
