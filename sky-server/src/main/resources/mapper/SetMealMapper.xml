<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hubu.mapper.SetMealMapper">
<!--    Integer countByCategoryId(Long id);-->
    <select id="countByCategoryId" >
        select count(id)
        from setmeal
        where category_id = #{id}
    </select>
<!--    Page<SetMeal> pageQuery(SetMealPageQueryDTO setMealPageQueryDTO);-->
    <select id="pageQuery" parameterType="com.hubu.dto.SetMealPageQueryDTO">
        select *
        from setmeal
        <where>
            <if test="name != null and name != ''">and name like concat ('%',#{name},'%')</if>
            <if test="categoryId != null">and category_id = #{categoryId}</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by update_time desc
    </select>
<!--    Long insert(SetMeal setMeal);-->
    <insert id="insert" parameterType="com.hubu.entity.SetMeal" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal(category_id,name,price,status,description,image,create_time,update_time,create_user,update_user)
        values (#{categoryId},#{name},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
<!--    @AutoFill(AutoFillEnum.UPDATE)-->
<!--    void update(SetMeal setMeal);-->
    <update id="update" parameterType="com.hubu.entity.SetMeal">
        update setmeal
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="name !=null and name!=''">name = #{name},</if>
            <if test="price != null ">price = #{price},</if>
            <if test="status !=null">status = #{status},</if>
            <if test="description!=null and description!=''">description=#{description},</if>
            <if test="image!=null and image!=''">image = #{image},</if>
            <if test="updateTime != null">update_time=#{updateTime},</if>
            <if test="updateUser!=null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
    </update>
<!--    SetMeal queryById(Long id);-->
    <select id="queryById" resultType="com.hubu.entity.SetMeal">
        select *
        from setmeal
        where id=#{id}
    </select>
<!--    void delete(Long id);-->
    <delete id="delete">
        delete from setmeal
        where id = #{id}
    </delete>
<!--    List<SetMeal> list(SetMeal setmeal);-->
    <select id="list" parameterType="com.hubu.entity.SetMeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
<!--    List<DishItemVO> getDishItemBySetMealId(Long id);-->
    <select id="getDishItemBySetMealId" resultType="com.hubu.vo.DishItemVO">
        select sd.name, sd.copies, d.image, d.description
        from setmeal_dish sd left join dish d on sd.dish_id = d.id
        where sd.setmeal_id = #{setmealId}
    </select>
</mapper>
